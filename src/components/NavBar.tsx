import React from "react";
import styled from "@emotion/styled";

export const NavBar = () => {

    return(
        <StyledNavBar>
            <></>
        </StyledNavBar>
    );
};

const StyledNavBar = styled.div`
    height: 160px;
    width: 100%;
`;